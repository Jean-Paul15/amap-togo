// Exports serveur (utilisent next/headers)
// Importer avec : import { createClientServer } from '@amap-togo/database/server'

export { createClientServer } from './client-server'
export { updateSession } from './middleware'
